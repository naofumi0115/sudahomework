UPDATE agents
SET COMMISSION = COMMISSION - 0.2
WHERE (agents.AGENT_CODE IN (
    SELECT customer.AGENT_CODE
    FROM customer
    WHERE customer.PAYMENT_AMT = (
        SELECT MIN(customer.PAYMENT_AMT)
        FROM customer)
    )
)
